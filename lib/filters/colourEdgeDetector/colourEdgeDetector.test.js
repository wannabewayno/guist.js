const colourEdgeDetector = require('./');

describe('colourEdgeDetector', () => {
    it('should ...', () => {
        const testValue = '...'
        const expectedResult = '...'
        expect(colourEdgeDetector(testValue)).toBe(expectedResult)
    })
})